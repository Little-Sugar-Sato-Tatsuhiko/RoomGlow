services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - TZ=Asia/Tokyo
    volumes:
      - .:/app
      - /app/node_modules
      - ./videos:/app/videos
      - ./data:/app/data
    command: npm run dev
